[tool.poetry]
name = "statistical_methods_library"
version = "3.5.6"
description = ""
authors = ["Your Name <you@example.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.7"
pyspark = "3.1.1"

[tool.poetry.dev-dependencies]
pytest = "^6.2.4"
black = "^21.11b0"
isort = "^5.10.1"
flake8 = "^4.0.1"
coverage = "^6.1"
pytest-cov = "^3.0.0"
chispa = "*"
pytest-dependency = "^0.5.1"
pytest-tap = "^3.3"
portray = "^1.7.0"
[tool.isort]
profile = "black"

[tool.pytest.ini_options]
addopts = "--exitfirst --tap-stream --cov=statistical_methods_library"

[tool.portray.mkdocs]
site_name = "Statistical Methods Library"
edit_uri = ""
repo_name = "Statistical Methods Library GitHub Repo"
site_url = "https://example.com/statistical-methods-library/"

[tool.portray.pdocs]
exclude_source = true
[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
